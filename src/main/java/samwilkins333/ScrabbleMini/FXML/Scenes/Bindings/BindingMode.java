package samwilkins333.ScrabbleMini.FXML.Scenes.Bindings;

/**
 * An enum that specifies the ways in which
 * a bean can be bound in GUI-related bindings.
 */
public enum BindingMode {
  UNIDIRECTIONAL,
  BIDIRECTIONAL,
  NONE
}
